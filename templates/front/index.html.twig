{% extends 'base_front.html.twig' %}

{% block body %}
    {% if is_granted('ROLE_COMPANY') %}
        {{ include('front/user/index/companyIndex.html.twig') }}
    {% elseif is_granted('ROLE_DRIVER') %}
        {{ include('front/driver/dashboard_driver/index.html.twig') }}
    {% elseif is_granted('ROLE_USER') %}
        {{ include('front/user/index/userIndex.html.twig') }}
    {% else %}
        {{ include('front/user/index/disconnectedUserIndex.html.twig') }}
    {% endif %}
{% endblock %}
